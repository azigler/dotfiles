# Per-host config lives in `~/.ssh/local` (gitignored, sourced via the Include
# below) — tailnet FQDNs, public IPs, identity files, anything we don't want
# leaking through the public dotfiles repo. Keep this committed file generic.

Include local
