// zig-zone tailnet ACL policy.
//
// Source of truth: this file. Paste into Tailscale admin → Access Controls
// when changed. The admin policy editor supports JSONC (comments).
//
// Identity model (Tailscale's, not invented here):
//   - `zig-computer` and `pico` are tagged `tag:server` — machine identities,
//     no human user behind them.
//   - `metis`, `zig`, and the iPhone are UNTAGGED — they authenticate as
//     the user's Tailscale account, so they match `autogroup:member`
//     (devices owned by a human Tailscale account).
//   - `autogroup:member` is src-only in Tailscale ACLs; `autogroup:self`
//     is the correct dst for "user's own other devices."
//
// Three rules cover the full traffic matrix:
//   1. server-to-server  (e.g. nginx → vs14-web via tailnet)
//   2. user-to-server    (SSH, Ollama API from a laptop)
//   3. user-to-self      (laptop ↔ phone over tailnet, e.g. AirDrop-style)
//
// Future contractor / co-owner devices not tied to the user's account
// match NO rule and are denied by default — allowlist-default-deny.
//
// SSH section: `check` action requires Tailscale-identity re-auth every 12h
// before opening a new SSH session. Existing sessions stay live.
//
// Two SSH rules:
//   1. autogroup:member → tag:server — clients (metis, iPhone) SSH into
//      servers. Users: `ubuntu` (the Linux user on zig-computer) or
//      autogroup:nonroot (any non-root user, e.g. `pico` on the Mac).
//      Action: `check` — re-auth every 12h (user identity exists to check).
//   2. tag:server → tag:server — server-to-server SSH (zig-computer ↔ pico).
//      Closes a workflow gap: the user lives on zig-computer (the workshop)
//      and needs to reach pico for ops/debug without bouncing through a
//      laptop. Action: `accept` — `check` ISN'T POSSIBLE here because the
//      source is a tagged device (machine identity, no human user to
//      re-authenticate). Tailscale rejects `check` with tag:server in src
//      ("'check' action does not support tags in src"). Security trade-off
//      acknowledged: if a tagged server is compromised, the attacker can
//      SSH into other tagged servers without re-auth. Bounded — server
//      compromise is already a large problem; this doesn't worsen the
//      blast radius materially.
//
// See spec bead `dotfiles-phe` § 4.2 for the rationale + § 6 OQs for
// decisions that shaped this. Server-to-server rule added post-spec as a
// real-world gap discovered during Phase 1 testing (bead dotfiles-qix).
//
// Taildrop file-sharing grant: Tailscale's default-deny ACL refuses
// Taildrop between devices owned by different users. tagged-server →
// user-device returns "peer is owned by a different user" — caught
// while trying to drop stable-diffusion-handoff.md to the iPhone. The
// `grants` block below opens bi-directional file-sharing across the
// full server↔user matrix (server↔server, server↔user, user↔server,
// user↔user). Capability-only grant (no L3 traffic implication;
// `ip: ["*"]` is required syntax). Use `tailscale file cp <file>
// <peer>:` once applied; received files surface in the iOS Files app
// under the Tailscale share location.

{
  "tagOwners": {
    "tag:server": ["autogroup:admin"]
  },

  "acls": [
    {
      "action": "accept",
      "src": ["tag:server"],
      "dst": ["tag:server:*"]
    },
    {
      "action": "accept",
      "src": ["autogroup:member"],
      "dst": ["tag:server:*"]
    },
    {
      "action": "accept",
      "src": ["autogroup:member"],
      "dst": ["autogroup:self:*"]
    }
  ],

  "grants": [
    {
      // Bi-directional Taildrop across the server↔user matrix.
      // Combined src+dst arrays mean OR — any listed src may send to
      // any listed dst. Covers tag:server↔tag:server, tag:server↔user,
      // user↔tag:server, and user↔user (the last is the default Taildrop
      // behavior; harmless to make explicit).
      "src": ["tag:server", "autogroup:member"],
      "dst": ["tag:server", "autogroup:member"],
      "ip":  ["*"],
      "app": {
        "tailscale.com/cap/file-sharing": [{}]
      }
    }
  ],

  "ssh": [
    {
      "action": "check",
      "src":    ["autogroup:member"],
      "dst":    ["tag:server"],
      "users":  ["ubuntu", "autogroup:nonroot"],
      // acceptEnv: Tailscale SSH doesn't passthrough env vars by default
      // (unlike OpenBSD sshd). Without this, TERM + LANG + LC_* from the
      // client are stripped, the remote shell falls back to POSIX locale,
      // and Unicode/nerdfont glyph rendering breaks. Especially visible
      // on iPhone Termius (bead dotfiles-st2). Requires Tailscale ≥ 1.76.
      "acceptEnv": ["TERM", "LANG", "LC_*"]
    },
    {
      "action": "accept",
      "src":    ["tag:server"],
      "dst":    ["tag:server"],
      "users":  ["autogroup:nonroot"],
      "acceptEnv": ["TERM", "LANG", "LC_*"]
    }
  ]
}
