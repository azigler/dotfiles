# Session handoff — 2026-08-10 724edc28 (THE CONSUL 🦅 → moving to ~/demesne)

> SCOPED NOTE (per-window, `consul`). Written at the dotfiles source and
> carried to demesne by the sync — **you are likely reading the demesne copy
> with `cwd=~/demesne`**: Zig is loading the consul there to OWN THE FLIP.
> The plain `session-handoff.md` belongs to the Works seat; never overwrite it.

## YOUR MISSION (Zig's charge, verbatim intent, 2026-08-10 ~02:3xZ)
**Drive `dotfiles-agent-brain-split-ezeu` — the demesne split (14 closed /
9 open) — to the flip.** What remains IS the flip itself:
- `860z` — retarget the six `~/.claude` symlinks
- `kvrl` — the systemd units (partially real already: units point at `~/.agents`)
- `n3b6` — sync.sh's seams
- `ocm7` — the cutover freeze window
- `tj3q` — demesne bootstrap identity
- `n77t` `tm0w` `qvee` `zga2` `b980` — de-hardcoding stragglers
Tonight advanced it materially: zsh/tmux joined the sync set, infra.md moved
to demesne root, and demesne can now SELF-VERIFY its own gate. After the
flip, the consuls move to ~/demesne because it becomes the edit surface —
finishing this epic is what makes your own new home legitimate.

## Operating notes for a demesne-homed consul (read before acting)
- **Beads live in the DOTFILES store** (demesne CLAUDE.md: no br init here).
  From `~/demesne`, run bead ops as `br --db /home/ubuntu/dotfiles/.beads
  <…>` or `cd /home/ubuntu/dotfiles` per call — verify which form the hooks
  tolerate before batching.
- **Until each seam flips, dotfiles stays canonical for the synced dirs**
  (agents/ refs/ claude/ zsh/ tmux/): an edit made only in demesne is
  OVERWRITTEN by the next demesne-sync. The flip work itself must sequence
  each seam's canonical-side change; the epic's beads define that order.
  infra.md and CLAUDE.md are demesne-native (outside the sync) — safe to
  edit in place.
- **Your project slug changes** (`-home-ubuntu-demesne`): fresh memory dir,
  fresh transcript lineage — this note is your ONLY continuity. The
  dotfiles-slug memory (MEMORY.md) does NOT follow; global tier does.
- The `sync-tick-settings` divergence warning at session start is healed
  (SONNET pin now in tick settings); if it re-fires, run the printed fix.

## State at offboard (2026-08-10 ~02:4xZ)
- dotfiles main `598f0e0`+, pushed; demesne clean at `8a56e29`+ (Works
  committed the 0.97 set). No in-flight subagents. All worktrees remaining
  belong to Works' lanes.
- **The whole estate is on tap:secondary** (zig@zigler.ai), each by a proven
  path: consul (manual alias launch, envelope verified), Works (seat-cycle
  take-2 — ORGANIC fable_ceiling=0.97 rollover, ledger triple 02:16:46Z +
  gateway group=secondary epoch-3), marshal (env pin, 1M window, fresh
  context). Fable leg live-fired twice (01:43Z throwaway + 02:16Z cycle).
- **Drain: pulse-marshal.timer fires 08:07Z (01:07 PT)**. Works holds the
  pre-satisfied 00:43 PT gate; 5gob's derived budget meters spend on the
  launch pool (secondary ≈ fresh) with cap transferred from primary tier
  history → full window. Zig's Fable weekly resets Thu 11pm PT; seats come
  home organically when primary's reading drops below 0.97.

## What happened (this stretch; fuller arc in this note's git history)
- **seat-cycle.sh born and proven** (`c80a5a5` + `974e029`, agents/bin):
  /exit+confirm → wrapper RE-SOURCE (stale-function fix, measured epoch-1
  mis-attribution without it) → canonical-model relaunch (bare `claude`
  never trips the argv-read fable leg) → readiness gate → opening prompt.
  Verified live on the Works seat, all three rollover legs.
- **Works window identity wrinkle** (`doto`, P1): my `--continue` relaunch
  grabbed the consul's transcript (shared slug, most-recent wins). RULED
  leave-in-place (seats are offices above sessions; occupant re-proved its
  state). Discipline: explicit `--resume <id>` or deliberate fresh, never
  bare `--continue` across shared-slug seats. Suite home: `0xru`
  (fleet-marked) incl. [continue-across-shared-slug].
- Earlier tonight (see git history of this note): t5fj closed; tap
  architecture end-to-end (kecb/glfx/rbci closed, epoch-3, Fable dimension
  discovered); consul seat registered (7ibz); lstn built+carried (OPEN —
  AC5 evidence = fresh marshal onboard pct, Works harvesting); o3qj closed
  (refused molts page Zig, second-in-90m); hall v1 tap cell; gu0o
  structural half (sync set widened); statusline docket fleet-marked.

## Friction
- Bare `--continue` cross-seat resume ambiguity → `doto`
- Stale wrapper functions in days-old pane shells → fixed in seat-cycle
  (`974e029`); systemic instance already covered by pulse-inject's idiom → one-off here
- seat-cycle shipped suite-less (gate banner, honest) → `0xru`
- restarter C-c pair sub-second timing → superseded by /exit verb in
  seat-cycle → one-off (mechanism retired)

## Decisions made this session (8 harvested, 44 scanned; window = full 08-09/10 arc)
- `7ibz` (consul topology — mine), `40ej` (outage charters — mine), `1aw8`
  (roster keeps aliases — my lane's builder)
- `kjjf` `bg71` `b01h` `zfgu` — Works-lane decisions (listed for the window)
- `t06l` — still OPEN, genuinely Zig's (bead-trailer exemption)
- Comment-recorded (not -t decision): doto leave-in-place ruling (on the bead)

## Proposed practices — where each landed
- "explicit --resume id, never bare --continue across seats" → `doto` AC
- "seat-cycle needs suite + gate mapping" → `0xru`
- none homeless.

## What's next (priority order)
1. **Onboard in ~/demesne** (this note + demesne CLAUDE.md + infra.md +
  `br --db /home/ubuntu/dotfiles/.beads show dotfiles-agent-brain-split-ezeu`).
  Then plan the flip: read the 9 open children (860z kvrl n3b6 ocm7 tj3q
  n77t tm0w qvee zga2 b980), sequence them, and drive — Fable plans,
  Opus/Sonnet implement, scrutiny gates, ocm7's freeze window for the
  flip moment itself.
2. `qwq9` (P1, seat_home dead on launch path) — fix shape + mutants on the
  bead; marshal's env pin comes OFF after the fix + one verified launch.
3. `lstn` close on Works' AC5 harvest; watch the drain ledger in the
  morning brief; `hzvi`/`3dyx` and the fleet-marked set ride the drain.
4. Then the standing docket: yrsg (re-scoped: restart-onto-next-tap),
  molt-lifecycle wave (inqj/betl), overhead wave (or6a/t9m7/w4ac/ie5a),
  h1oa pulse OQs (Zig-gated).

## Warnings / watch-outs
- **Demesne-homed editing discipline** (top of this note) — the flip is
  precisely the work of moving canonicality; until a seam flips, its edit
  belongs in dotfiles. Getting this backwards silently loses work to the
  next sync.
- Escalate pages Zig on a second refused/failed molt within 90m — molt
  deliberately, mind the window.
- gu0o's exit-code half (NOT-RUN mutant → all-killed line) is still unfixed
  — treat demesne gate summaries with that suspicion until it lands.
- The kuog inject-suite flake bites concurrent gates; retry from quiet, never bypass.
- Works' current occupant is a fork of the consul transcript acting as
  Works (doto) — fully briefed, ruled legitimate; don't "correct" it.
