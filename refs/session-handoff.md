# Session handoff — 2026-08-09, session d7033a72 (dotfiles, personal tap) — MOLT: ALL BUILDERS LANDED

> Post-molt context: the founding arc's BUILD phase is DONE. Zero worktrees, main clean+pushed.
> What remains is EXECUTION under Zig's standing authority (ezeu): migration -> cutover -> maiden.
> Stop only on a failed gate. This is a MOLT mode cycle (--in-flight no): /clear + /onboard.

## EVERYTHING IS BUILT (verify: `git log --oneline -40`, all merged)
The estate: constitution ratified; 18 seated careers w/ founding histories; hall LIVE (Ctrl-b W,
responsive + input-fixed + visit-launches); seneschal (brief timer 13:44 UTC today); MARSHAL BUILT
(budget calculator, br-scheduler ranking, /marshal skill — NOT armed, maiden is manual); molt LIVE
w/ turn-end signal (this session just used it); laurels machinery (awaiting first placement);
epoch-2 attribution; two taps + tick-profile (billing-true); the br/bv audit wave (11 findings
landed, labels real, #418 filed); works audited/patched/backed-up. Post spec (7fik) + comms
doctrine (d54d) authored, NOT built (post-cutover). harnessd campaign: peer-owned, 12 fleet-labeled
beads, plan-of-record harnessd-mo5l, lman ratified — the marshal's MAIDEN target.

## THE REMAINING SEQUENCE (do this, in order, stop on any failed gate)
1. **SCHEMA MIGRATION (ri8b)** — NOW SAFE (no builders in flight). br 0.2.16->0.2.22 (backups
   ~/.local/bin/br.bak-0.2.16); per store: `br doctor migrate-schema plan` -> review -> apply ->
   `br ready` verifies. Stores: dotfiles harnessd demesne vs14d linearb hevyd explore picod aaif
   (+ sweep ~ for .beads dirs). WARN the peer session before mutating shared stores. bv version
   mystery: the bead notes upstream tags don't match installed — investigate or defer bv.
2. **THE CUTOVER** (epic ezeu; freeze re-derive first — the unit set grew tonight):
   demesne-freeze.sh -> demesne-sync.sh + --gate (exit 0 REQUIRED, the identity gate) ->
   860z flip (evict aaif lock per 32j4 -> ~/.agents->~/demesne -> six ~/.claude links -> neuter
   sync.sh clobber + session-start re-assert) -> n3b6+6ttp -> kvrl (re-derive, ~66 units) ->
   n77t+5tn3 -> zwvu serialized -> unfreeze -> /clear ALL windows -> soak -> zga2(P0)+vtx4+k579
   one motion -> pvlp+1vpm.
3. **SUPERVISED MAIDEN MARSHAL RUN** — manual, in the marshal window, Zig watching:
   `~/dotfiles/agents/scheduler/pulse-inject.sh --loop pulse-marshal --dir ~/dotfiles
   --session zig-computer --window marshal --fresh --cmd "/marshal night"`. Drains the 12
   harnessd beads. Seams -> beads -> dispatched fixes (Zig's endgame directive), until
   dotfiles+demesne+harnessd is all done.

## TONIGHT'S CLOCKWORK (armed, fires during the above — the intended stress test)
- Dream tick ~11:13 UTC: maiden FLEET dream + possible FIRST LAUREL. Verify: ledger row slug
  '(fleet)' + n_slugs/denied; gateway model = d0bk's CLOSING EVIDENCE (close d0bk THEN faty on it).
- One-shot cron 4373abdb ~11:37 UTC: verifies the dream, THEN pico Tahoe upgrade per xh18
  (payload downloaded, $PICO_LOGIN_PASSWORD staged+verified in pico:~/.secrets, autonomous,
  post-checklist: ports/gateway-401/vs14-200/colima-autostart/RomM).

## AWAITING ZIG (non-blocking)
pm33 /check walk (9 OQs — mostly resolved by the budget amendment + endgame; qmrp D1-D7 at the
post-flip pause); hall device-verify (hnhl stays open); marshal weekly_cap_tokens (else 150k floor).

## FRICTION THIS ARC (all -> mechanism)
duplicate-dispatch->yoyb · premature-reap->3135 (guard live, fired twice correctly) · anchor
conflicts->69qr R3b · png-swept-into-my-commit->stage with explicit pathspec (lesson, no bead) ·
molt-blind-under-counters->ygf8 (fixed). Decisions on beads (`br list -t decision`).
