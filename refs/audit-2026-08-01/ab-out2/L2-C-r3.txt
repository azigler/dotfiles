The repo is empty — one 49-byte `README.md`, one commit, no source files, no language, no build system, no existing config code. So the dispatch prompt would be almost entirely invented rather than grounded in the codebase. Here's exactly what I'd launch, followed by the gaps you should fill before I do.

## The call

```
Agent({
  description: "Implement config parser",
  subagent_type: "general-purpose",
  name: "config-parser",
  run_in_background: false,
  prompt: `
Implement a new config parser module in this repo (currently empty except README.md).

Scope:
- Create the module that loads and parses config files, plus its tests.
- Validate required keys, apply defaults, and surface clear errors with file + key context.
- Do NOT add dependencies beyond the standard library without saying so in your report.

Commit:
- The repo is on 'main', which is the default branch. Create a branch first
  (e.g. 'feat/config-parser'), then commit there. Do not push.
- One commit, conventional-commit subject, body explaining the format and error model.

Report back: files created, the config format you implemented, public API surface,
how errors are represented, and anything you had to guess.
  `.trim()
})
```

`run_in_background: false` because you'll want to see the result before anything else moves; `general-purpose` because it needs write + Bash for the commit (`Explore` and `Plan` are read-only and can't do either).

## What I'd need from you first

Three choices that would each change the entire output, and I can't infer any of them from an empty repo:

1. **Language / runtime** — Python, Go, TypeScript, Rust? Determines everything including the test framework.
2. **Config format** — TOML, YAML, JSON, INI, or env-var overlay? This is the actual substance of a "parser."
3. **"New" implies an old one** — is there a config parser elsewhere this replaces or mirrors? If so, point me at it; that's a far better anchor than a spec I write.

Without those, the agent will pick for you and you'll likely get something you have to throw away. Give me the three and I'll fire the call above with the specifics substituted in.

One unrelated thing I noticed: `README.md:3` has "recieve" → "receive". Not touching it since it's outside what you asked for.
