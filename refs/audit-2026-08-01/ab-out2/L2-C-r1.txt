Repo is bare: one commit, one file (`README.md`, 3 lines). No source, no language, no build system, nothing named "config" anywhere. So "the new config parser module" has no referent I can resolve from the code — language, config format, and module location would all be invented by whoever I dispatch, and then committed.

I have not launched anything. Here is the exact call I'd make:

```js
Agent({
  description: "Implement config parser",
  subagent_type: "general-purpose",
  name: "config-parser",
  run_in_background: false,
  prompt: `Implement a new config parser module in this repo and commit it.

REPO STATE: essentially empty — one commit (28a252f "init"), one file (README.md).
You are establishing conventions, not matching them.

SCOPE:
- Language/runtime: <PYTHON | TYPESCRIPT | GO — FILL IN>
- Config format to parse: <TOML | YAML | JSON | INI — FILL IN>
- Module path: <e.g. src/config/parser.py — FILL IN>
- Behavior: load file -> validate required keys -> apply defaults ->
  return a typed config object. Raise a clear, typed error on malformed
  input or missing required keys. No env-var layering, no CLI wiring,
  no hot-reload unless asked.

REQUIREMENTS:
- Unit tests covering: valid parse, malformed syntax, missing required
  key, defaults applied. Tests must actually run and pass.
- Standard library only unless the chosen format requires a parser dep;
  if you add a dep, add the manifest file too and say why.

COMMIT:
- We are on main. Create a branch first: git checkout -b feat/config-parser
- Commit locally only. Do NOT push. Do NOT open a PR.
- Run the tests before committing; if they fail, fix or report — do not
  commit failing tests.

REPORT BACK: files created, the commit SHA, test output, and every
design decision you made that wasn't specified above.`
})
```

Three things are baked in as guardrails rather than left to the agent: branch off `main` before committing, local commit only (no push/PR), and tests must pass pre-commit.

The three `<FILL IN>` slots are the blocker. They aren't stylistic — language and format determine every line the agent writes, and a wrong guess means an invented module sitting in a commit you have to unwind. If you want a default rather than a decision: Python + TOML via `tomllib` at `src/config/parser.py`, stdlib-only. Say "go with the defaults" or give me the three values and I'll dispatch it as written.
